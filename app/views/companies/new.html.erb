<section class="page-hero relative">
  <span class="page-kicker">Company onboarding</span>
  <h1 class="page-title">Register your company for recruiter access.</h1>
  <p class="page-subtitle">Create a cleaner company profile up front so approval and job publishing move faster after review.</p>
</section>

<section class="section-card max-w-5xl mx-auto">
  <%= form_with model: @company, local: true, class: "space-y-8" do |f| %>
    <% if @company.errors.any? %>
      <div class="flash-banner alert">
        <div>
          <p class="font-semibold">Please review these fields</p>
          <ul class="mt-2 list-disc pl-5">
            <% @company.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      </div>
    <% end %>

    <div class="grid gap-8 lg:grid-cols-2">
      <div class="section-card-muted space-y-5">
        <h2 class="text-xl font-bold text-slate-900">Company details</h2>

        <div>
          <%= f.label :name, class: "mb-2 block text-sm font-semibold text-slate-700" %>
          <%= f.text_field :name, required: true %>
        </div>

        <div>
          <%= f.label :description, class: "mb-2 block text-sm font-semibold text-slate-700" %>
          <%= f.text_area :description, rows: 7 %>
        </div>
      </div>

      <div class="space-y-6">
        <div class="section-card-muted grid gap-4 md:grid-cols-2">
          <div>
            <%= f.label :location, class: "mb-2 block text-sm font-semibold text-slate-700" %>
            <%= f.text_field :location %>
          </div>
          <div>
            <%= f.label :website, class: "mb-2 block text-sm font-semibold text-slate-700" %>
            <%= f.url_field :website, placeholder: "https://" %>
          </div>
          <div>
            <%= f.label :industry, class: "mb-2 block text-sm font-semibold text-slate-700" %>
            <%= f.select :industry, ["Technology", "Healthcare", "Finance", "Education", "Retail", "Manufacturing", "Other"].map { |i| [i, i] }, { prompt: "Select industry" } %>
          </div>
          <div>
            <%= f.label :size, "Company size", class: "mb-2 block text-sm font-semibold text-slate-700" %>
            <%= f.select :size, Company::SIZES.map { |size| [size, size] }, { prompt: "Select size" } %>
          </div>
        </div>

        <div class="section-card-muted">
          <%= f.label :logo, class: "mb-2 block text-sm font-semibold text-slate-700" %>
          <%= f.file_field :logo, accept: "image/*" %>
          <p class="mt-2 text-sm text-slate-500">PNG or JPG. A strong company mark helps the job cards feel complete.</p>
        </div>

        <div class="section-card-muted">
          <h3 class="text-lg font-bold text-slate-900">What happens next</h3>
          <ul class="mt-4 space-y-2 text-sm leading-7 text-slate-600">
            <li>Your company profile is reviewed by an admin.</li>
            <li>You’ll get notified once it is approved.</li>
            <li>Approved companies can immediately begin posting jobs.</li>
          </ul>
        </div>
      </div>
    </div>

    <div class="flex flex-col gap-3 border-t border-slate-200 pt-6 sm:flex-row sm:justify-end">
      <%= link_to "Cancel", root_path, class: "btn-secondary" %>
      <%= f.submit "Submit for approval", class: "btn-primary" %>
    </div>
  <% end %>
</section>
