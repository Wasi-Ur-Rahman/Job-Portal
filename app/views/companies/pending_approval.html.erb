<section class="status-panel">
  <div class="status-panel-icon">
    <svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
  </div>

  <span class="badge-pill badge-amber">Pending review</span>
  <h1 class="mt-4 text-4xl font-bold text-slate-900">Your company profile is under review.</h1>
  <p class="mt-3 text-sm leading-7 text-slate-600">An admin is checking the company details before recruiter access is activated.</p>

  <% if @companies.any? %>
    <div class="mt-8 space-y-4 text-left">
      <% @companies.each do |company| %>
        <div class="section-card-muted">
          <p class="text-lg font-bold text-slate-900"><%= company.name %></p>
          <p class="mt-1 text-sm text-slate-500">Submitted <%= company.created_at.strftime("%B %d, %Y") %></p>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="mt-8 flex flex-wrap justify-center gap-3">
    <%= link_to "Update profile", edit_user_registration_path, class: "btn-secondary" %>
    <%= link_to "Browse jobs", jobs_path, class: "btn-primary" %>
  </div>
</section>
