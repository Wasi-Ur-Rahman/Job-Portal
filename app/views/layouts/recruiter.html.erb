<!DOCTYPE html>
<html>
  <head>
    <title>Job Portal - Recruiter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= turbo_refreshes_with method: :morph, scroll: :preserve %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <%= turbo_stream_from notification_stream_name(current_user) %>
    <% end %>

    <nav class="topbar" data-controller="mobile-nav">
      <div class="topbar-shell">
        <div class="topbar-row">
          <div class="flex items-center gap-3">
            <%= link_to recruiter_dashboard_path, class: "topbar-brand" do %>
              <span class="topbar-brand-mark">RC</span>
              <span>Recruiter Hub</span>
            <% end %>

            <div class="topbar-nav">
              <%= link_to "Dashboard", recruiter_dashboard_path, class: "topbar-link #{'active' if current_page?(recruiter_dashboard_path)}" %>
              <%= link_to "Jobs", recruiter_jobs_path, class: "topbar-link #{'active' if controller_name == 'jobs'}" %>
              <%= link_to "Applications", recruiter_applications_path, class: "topbar-link #{'active' if controller_name == 'applications'}" %>
              <% if current_user.can_manage_company?(current_company) %>
                <%= link_to "Team", recruiter_team_index_path, class: "topbar-link #{'active' if controller_name == 'team'}" %>
                <%= link_to "Company", recruiter_company_path(current_company), class: "topbar-link #{'active' if controller_name == 'companies'}" %>
              <% end %>
            </div>
          </div>

          <div class="topbar-actions">
            <%= render "shared/notification_badge", user: current_user %>
            <div class="topbar-meta">
              <div class="text-right">
                <p class="text-xs uppercase tracking-[0.18em] text-slate-400"><%= current_company&.name || "Company" %></p>
                <p class="text-sm font-semibold text-slate-800"><%= current_user.first_name %></p>
              </div>
              <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn-secondary" %>
            </div>
          </div>

          <button type="button" class="topbar-mobile-toggle" data-mobile-nav-target="button" data-action="mobile-nav#toggle" aria-expanded="false" aria-label="Toggle navigation">
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M4 7h16M4 12h16M4 17h16" />
            </svg>
          </button>
        </div>

        <div class="topbar-mobile-panel hidden" data-mobile-nav-target="panel">
          <div class="topbar-mobile-stack">
            <%= link_to "Dashboard", recruiter_dashboard_path, class: "topbar-link #{'active' if current_page?(recruiter_dashboard_path)}", data: { action: "mobile-nav#close" } %>
            <%= link_to "Jobs", recruiter_jobs_path, class: "topbar-link #{'active' if controller_name == 'jobs'}", data: { action: "mobile-nav#close" } %>
            <%= link_to "Applications", recruiter_applications_path, class: "topbar-link #{'active' if controller_name == 'applications'}", data: { action: "mobile-nav#close" } %>
            <% if current_user.can_manage_company?(current_company) %>
              <%= link_to "Team", recruiter_team_index_path, class: "topbar-link #{'active' if controller_name == 'team'}", data: { action: "mobile-nav#close" } %>
              <%= link_to "Company", recruiter_company_path(current_company), class: "topbar-link #{'active' if controller_name == 'companies'}", data: { action: "mobile-nav#close" } %>
            <% end %>
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete, action: "mobile-nav#close" }, class: "btn-primary w-full" %>
          </div>
        </div>
      </div>
    </nav>

    <main class="app-shell">
      <% if notice || alert %>
        <div class="flash-stack">
          <% if notice %>
            <div class="flash-banner notice"><div><p class="font-semibold">Success</p><p><%= notice %></p></div></div>
          <% end %>
          <% if alert %>
            <div class="flash-banner alert"><div><p class="font-semibold">Attention</p><p><%= alert %></p></div></div>
          <% end %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
