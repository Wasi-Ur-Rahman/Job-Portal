<section class="page-hero relative">
  <span class="page-kicker">Inbox</span>
  <h1 class="page-title">Notifications without the clutter.</h1>
  <p class="page-subtitle"><%= pluralize(@unread_count, "unread notification") %> waiting for you.</p>
</section>

<%= render "panel", notifications: @notifications, unread_count: @unread_count, user: current_user %>
